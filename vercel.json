{
  "framework": "nextjs",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.29.3 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter website run build",
  "outputDirectory": ".next"
}
